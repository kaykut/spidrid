diff --git a/node_modules/expo-share-intent/android/src/main/java/expo/modules/shareintent/ExpoShareIntentModule.kt b/node_modules/expo-share-intent/android/src/main/java/expo/modules/shareintent/ExpoShareIntentModule.kt
index 0153c89..dd8156c 100644
--- a/node_modules/expo-share-intent/android/src/main/java/expo/modules/shareintent/ExpoShareIntentModule.kt
+++ b/node_modules/expo-share-intent/android/src/main/java/expo/modules/shareintent/ExpoShareIntentModule.kt
@@ -13,6 +13,7 @@ import android.content.ContentResolver
 import android.content.ContentUris
 import android.content.Context
 import android.content.Intent
+import android.content.pm.PackageManager
 import android.database.Cursor
 import android.graphics.BitmapFactory
 import android.media.MediaMetadataRetriever;
@@ -122,6 +123,7 @@ class ExpoShareIntentModule : Module() {
                 return
             }
             if (intent.type == null) return
+            val shareTarget = resolveShareTarget(intent)
             if (intent.type!!.startsWith("text/plain")) {
                 // text / urls
                 if (intent.action == Intent.ACTION_SEND) {
@@ -131,9 +133,11 @@ class ExpoShareIntentModule : Module() {
                         "meta" to mapOf(
                             "title" to intent.getCharSequenceExtra(Intent.EXTRA_TITLE),
                         )
+                        ,
+                        "shareTarget" to shareTarget
                     ))
                 } else if (intent.action == Intent.ACTION_VIEW) {
-                    notifyShareIntent(mapOf( "text" to intent.dataString, "type" to "text"))
+                    notifyShareIntent(mapOf( "text" to intent.dataString, "type" to "text", "shareTarget" to shareTarget))
                 } else {
                     notifyError("Invalid action for text sharing: " + intent.action)
                 }
@@ -142,14 +146,14 @@ class ExpoShareIntentModule : Module() {
                 if (intent.action == Intent.ACTION_SEND) {
                     val uri = intent.parcelable<Uri>(Intent.EXTRA_STREAM);
                     if (uri != null) {
-                        notifyShareIntent(mapOf( "files" to arrayOf(getFileInfo(uri), "type" to "file")))
+                        notifyShareIntent(mapOf( "files" to arrayOf(getFileInfo(uri), "type" to "file"), "shareTarget" to shareTarget))
                     } else {
                         notifyError("empty uri for file sharing: " + intent.action)
                     }
                 } else if (intent.action == Intent.ACTION_SEND_MULTIPLE) {
                     val uris = intent.parcelableArrayList<Uri>(Intent.EXTRA_STREAM)
                     if (uris != null) {
-                        notifyShareIntent(mapOf( "files" to uris.map { getFileInfo(it) }, "type" to "file"))
+                        notifyShareIntent(mapOf( "files" to uris.map { getFileInfo(it) }, "type" to "file", "shareTarget" to shareTarget))
                     } else {
                         notifyError("empty uris array for file sharing: " + intent.action)
                     }
@@ -159,6 +163,31 @@ class ExpoShareIntentModule : Module() {
             }
         }
 
+        private fun resolveShareTarget(intent: Intent): String? {
+            val component = intent.component ?: instance?.currentActivity?.componentName
+            if (component != null) {
+                try {
+                    val info = instance?.context?.packageManager?.getActivityInfo(component, PackageManager.GET_META_DATA)
+                    val metaTarget = info?.metaData?.getString("devoroShareTarget")
+                    if (!metaTarget.isNullOrEmpty()) {
+                        return metaTarget
+                    }
+                } catch (_: Exception) {
+                    // Ignore meta-data lookup failures
+                }
+
+                val className = component.className ?: ""
+                if (className.contains("ShareToDevoroAdd")) {
+                    return "add"
+                }
+                if (className.contains("ShareToDevoroOpen")) {
+                    return "open"
+                }
+            }
+
+            return intent.getStringExtra("devoroShareTarget")
+        }
+
         /*
          * https://stackoverflow.com/questions/73019160/the-getparcelableextra-method-is-deprecated
          */
diff --git a/node_modules/expo-share-intent/build/ExpoShareIntentModule.types.d.ts b/node_modules/expo-share-intent/build/ExpoShareIntentModule.types.d.ts
index f2ebd1a..ea3c115 100644
--- a/node_modules/expo-share-intent/build/ExpoShareIntentModule.types.d.ts
+++ b/node_modules/expo-share-intent/build/ExpoShareIntentModule.types.d.ts
@@ -45,6 +45,7 @@ export type ShareIntentMeta = Record<string, string | undefined> & {
 interface BaseShareIntent {
     meta?: ShareIntentMeta | null;
     text?: string | null;
+    shareTarget?: string | null;
 }
 /**
  * Shared intent to represent both platforms.
diff --git a/node_modules/expo-share-intent/build/useShareIntent.js b/node_modules/expo-share-intent/build/useShareIntent.js
index 1ab12c4..a8c60f5 100644
--- a/node_modules/expo-share-intent/build/useShareIntent.js
+++ b/node_modules/expo-share-intent/build/useShareIntent.js
@@ -8,6 +8,7 @@ export const SHAREINTENT_DEFAULTVALUE = {
     text: null,
     webUrl: null,
     type: null,
+    shareTarget: null,
 };
 export const SHAREINTENT_OPTIONS_DEFAULT = {
     debug: false,
@@ -15,6 +16,16 @@ export const SHAREINTENT_OPTIONS_DEFAULT = {
     disabled: Platform.OS === "web",
 };
 const isValueAvailable = (shareIntent) => !!(shareIntent?.text || shareIntent?.webUrl || shareIntent?.files);
+const resolveShareExtensionKey = (url, options) => {
+    if (!url) {
+        return getShareExtensionKey(options);
+    }
+    const match = url.match(/dataUrl=([^?#]+)/);
+    if (match?.[1]) {
+        return decodeURIComponent(match[1]);
+    }
+    return getShareExtensionKey(options);
+};
 export default function useShareIntent(options = SHAREINTENT_OPTIONS_DEFAULT) {
     const url = useLinkingURL();
     const appState = useRef(AppState.currentState);
@@ -25,8 +36,8 @@ export default function useShareIntent(options = SHAREINTENT_OPTIONS_DEFAULT) {
         if (options.disabled)
             return;
         setError(null);
-        clearNativeModule &&
-            ExpoShareIntentModule?.clearShareIntent(getShareExtensionKey(options));
+        const shareKey = resolveShareExtensionKey(url, options);
+        clearNativeModule && ExpoShareIntentModule?.clearShareIntent(shareKey);
         if (isValueAvailable(shareIntent)) {
             setSharedIntent(SHAREINTENT_DEFAULTVALUE);
             options.onResetShareIntent?.();
diff --git a/node_modules/expo-share-intent/build/utils.js b/node_modules/expo-share-intent/build/utils.js
index 3d343c6..946658f 100644
--- a/node_modules/expo-share-intent/build/utils.js
+++ b/node_modules/expo-share-intent/build/utils.js
@@ -57,6 +57,7 @@ export const parseShareIntent = (value, options) => {
     else {
         shareIntent = value; // Android
     }
+    const shareTarget = shareIntent?.shareTarget ?? null;
     if (shareIntent?.text) {
         // Try to find the webURL in the SharedIntent text
         const webUrl = shareIntent.text
@@ -70,6 +71,7 @@ export const parseShareIntent = (value, options) => {
             meta: {
                 title: shareIntent.meta?.title ?? undefined,
             },
+            shareTarget,
         };
     }
     else if (shareIntent?.weburls?.length) {
@@ -80,6 +82,7 @@ export const parseShareIntent = (value, options) => {
             text: weburl.url, // retrocompatibility
             webUrl: weburl.url,
             meta: parseJson(weburl.meta, {}),
+            shareTarget,
         };
     }
     else {
@@ -112,6 +115,7 @@ export const parseShareIntent = (value, options) => {
                 }, [])
                 : null,
             type: isMedia ? "media" : "file",
+            shareTarget,
         };
     }
     options.debug &&
diff --git a/node_modules/expo-share-intent/plugin/build/ios/ShareExtensionViewController.swift b/node_modules/expo-share-intent/plugin/build/ios/ShareExtensionViewController.swift
index 0911a9e..ac7b4d2 100644
--- a/node_modules/expo-share-intent/plugin/build/ios/ShareExtensionViewController.swift
+++ b/node_modules/expo-share-intent/plugin/build/ios/ShareExtensionViewController.swift
@@ -12,7 +12,7 @@ import UIKit
 class ShareViewController: UIViewController {
   let hostAppGroupIdentifier = "<GROUPIDENTIFIER>"
   let shareProtocol = "<SCHEME>"
-  let sharedKey = "<SCHEME>ShareKey"
+  let sharedKey = "<SCHEME>ShareKey<SHARE_KEY_SUFFIX>"
   var sharedMedia: [SharedMediaFile] = []
   var sharedWebUrl: [WebUrl] = []
   var sharedText: [String] = []
@@ -478,7 +478,7 @@ class ShareViewController: UIViewController {
   }
 
   private func redirectToHostApp(type: RedirectType) {
-    let url = URL(string: "\(shareProtocol)://dataUrl=\(sharedKey)#\(type)")!
+    let url = URL(string: "\(shareProtocol)://dataUrl=\(sharedKey)?target=<SHARE_TARGET>#\(type)")!
     var responder = self as UIResponder?
 
     while responder != nil {
diff --git a/node_modules/expo-share-intent/plugin/build/ios/withIosShareExtensionXcodeTarget.js b/node_modules/expo-share-intent/plugin/build/ios/withIosShareExtensionXcodeTarget.js
index eb5971b..1856063 100644
--- a/node_modules/expo-share-intent/plugin/build/ios/withIosShareExtensionXcodeTarget.js
+++ b/node_modules/expo-share-intent/plugin/build/ios/withIosShareExtensionXcodeTarget.js
@@ -29,6 +29,26 @@ function getMainAppDevelopmentTeam(pbx) {
     console.warn("[expo-share-intent] No DEVELOPMENT_TEAM found in main app build settings. Developer will need to manually add Dev Team.");
     return null;
 }
+//───────────────────────────────────────────────────────────────────────────
+// Helper: Generate a random UUID for xcode project
+//───────────────────────────────────────────────────────────────────────────
+function generateUuid() {
+    return 'xxxxxxxxxxxxxxxxxxxxxxxx'.replace(/x/g, function() {
+        return Math.floor(Math.random() * 16).toString(16).toUpperCase();
+    });
+}
+//───────────────────────────────────────────────────────────────────────────
+// Helper: Get file type for Xcode
+//───────────────────────────────────────────────────────────────────────────
+function getFileType(fileName) {
+    if (fileName.endsWith('.swift')) return 'sourcecode.swift';
+    if (fileName.endsWith('.storyboard')) return 'file.storyboard';
+    if (fileName.endsWith('.js')) return 'sourcecode.javascript';
+    if (fileName.endsWith('.plist')) return 'text.plist.xml';
+    if (fileName.endsWith('.entitlements')) return 'text.plist.entitlements';
+    if (fileName.endsWith('.xcprivacy')) return 'text.xml';
+    return 'file';
+}
 const withShareExtensionXcodeTarget = (config, parameters) => {
     return (0, config_plugins_1.withXcodeProject)(config, async (config) => {
         const extensionName = (0, constants_1.getShareExtensionName)(parameters);
@@ -75,32 +95,130 @@ const withShareExtensionXcodeTarget = (config, parameters) => {
         ];
         const allFiles = [...sourceFiles, ...resourceFiles, ...configFiles];
         /* ------------------------------------------------------------------ */
-        /* 4. Create target, group & build phases (CORRECTED APPROACH)       */
+        /* 4. Create target, group & build phases                             */
+        /*    FIX: Create unique file references for each extension           */
         /* ------------------------------------------------------------------ */
-        // 4.1 Create PBXGroup for the extension using addPbxGroup (OneSignal style)
-        const extGroup = pbxProject.addPbxGroup(allFiles, extensionName, extensionName);
-        // 4.2 Add the new PBXGroup to the top level group
-        const groups = pbxProject.hash.project.objects.PBXGroup;
+        const projObjects = pbxProject.hash.project.objects;
+
+        // 4.1 Create unique file references for this extension
+        const fileRefs = {};
+        projObjects.PBXFileReference = projObjects.PBXFileReference || {};
+
+        for (const fileName of allFiles) {
+            const uuid = generateUuid();
+            projObjects.PBXFileReference[uuid] = {
+                isa: 'PBXFileReference',
+                lastKnownFileType: getFileType(fileName),
+                path: fileName,
+                sourceTree: '"<group>"',
+                name: fileName,
+            };
+            projObjects.PBXFileReference[uuid + '_comment'] = fileName;
+            fileRefs[fileName] = uuid;
+        }
+
+        // 4.2 Create PBXGroup for the extension with our file references
+        const groupUuid = generateUuid();
+        projObjects.PBXGroup = projObjects.PBXGroup || {};
+        projObjects.PBXGroup[groupUuid] = {
+            isa: 'PBXGroup',
+            children: allFiles.map(f => ({ value: fileRefs[f], comment: f })),
+            name: extensionName,
+            path: extensionName,
+            sourceTree: '"<group>"',
+        };
+        projObjects.PBXGroup[groupUuid + '_comment'] = extensionName;
+
+        // 4.3 Add the new PBXGroup to the top level (main) group
+        const groups = projObjects.PBXGroup;
         Object.keys(groups).forEach(function (key) {
             if (typeof groups[key] === "object" &&
                 groups[key].name === undefined &&
-                groups[key].path === undefined) {
-                pbxProject.addToPbxGroup(extGroup.uuid, key);
+                groups[key].path === undefined &&
+                groups[key].children) {
+                // This is the main project group
+                groups[key].children.push({ value: groupUuid, comment: extensionName });
             }
         });
-        // 4.3 WORK AROUND for addTarget BUG (from OneSignal)
-        // Xcode projects don't contain these if there is only one target
-        const projObjects = pbxProject.hash.project.objects;
+
+        // 4.4 WORK AROUND for addTarget BUG (from OneSignal)
         projObjects.PBXTargetDependency = projObjects.PBXTargetDependency || {};
         projObjects.PBXContainerItemProxy = projObjects.PBXContainerItemProxy || {};
-        // 4.4 Create native target
+
+        // 4.5 Create native target
         const target = pbxProject.addTarget(extensionName, "app_extension", extensionName);
-        // 4.5 Add build phases to the new target
-        pbxProject.addBuildPhase(sourceFiles, // Add source files directly to the build phase
-        "PBXSourcesBuildPhase", "Sources", target.uuid);
-        pbxProject.addBuildPhase(resourceFiles, // Add resource files directly to the build phase
-        "PBXResourcesBuildPhase", "Resources", target.uuid);
-        pbxProject.addBuildPhase([], "PBXFrameworksBuildPhase", "Frameworks", target.uuid);
+
+        // 4.6 Create build phases manually with our specific file UUIDs
+        const sourcesPhaseUuid = generateUuid();
+        const resourcesPhaseUuid = generateUuid();
+        const frameworksPhaseUuid = generateUuid();
+
+        projObjects.PBXSourcesBuildPhase = projObjects.PBXSourcesBuildPhase || {};
+        projObjects.PBXSourcesBuildPhase[sourcesPhaseUuid] = {
+            isa: 'PBXSourcesBuildPhase',
+            buildActionMask: 2147483647,
+            files: [],
+            runOnlyForDeploymentPostprocessing: 0,
+        };
+        projObjects.PBXSourcesBuildPhase[sourcesPhaseUuid + '_comment'] = 'Sources';
+
+        projObjects.PBXResourcesBuildPhase = projObjects.PBXResourcesBuildPhase || {};
+        projObjects.PBXResourcesBuildPhase[resourcesPhaseUuid] = {
+            isa: 'PBXResourcesBuildPhase',
+            buildActionMask: 2147483647,
+            files: [],
+            runOnlyForDeploymentPostprocessing: 0,
+        };
+        projObjects.PBXResourcesBuildPhase[resourcesPhaseUuid + '_comment'] = 'Resources';
+
+        projObjects.PBXFrameworksBuildPhase = projObjects.PBXFrameworksBuildPhase || {};
+        projObjects.PBXFrameworksBuildPhase[frameworksPhaseUuid] = {
+            isa: 'PBXFrameworksBuildPhase',
+            buildActionMask: 2147483647,
+            files: [],
+            runOnlyForDeploymentPostprocessing: 0,
+        };
+        projObjects.PBXFrameworksBuildPhase[frameworksPhaseUuid + '_comment'] = 'Frameworks';
+
+        // 4.7 Add files to build phases using our unique UUIDs
+        projObjects.PBXBuildFile = projObjects.PBXBuildFile || {};
+
+        for (const fileName of sourceFiles) {
+            const buildFileUuid = generateUuid();
+            projObjects.PBXBuildFile[buildFileUuid] = {
+                isa: 'PBXBuildFile',
+                fileRef: fileRefs[fileName],
+                fileRef_comment: fileName,
+            };
+            projObjects.PBXBuildFile[buildFileUuid + '_comment'] = `${fileName} in Sources`;
+            projObjects.PBXSourcesBuildPhase[sourcesPhaseUuid].files.push({
+                value: buildFileUuid,
+                comment: `${fileName} in Sources`
+            });
+        }
+
+        for (const fileName of resourceFiles) {
+            const buildFileUuid = generateUuid();
+            projObjects.PBXBuildFile[buildFileUuid] = {
+                isa: 'PBXBuildFile',
+                fileRef: fileRefs[fileName],
+                fileRef_comment: fileName,
+            };
+            projObjects.PBXBuildFile[buildFileUuid + '_comment'] = `${fileName} in Resources`;
+            projObjects.PBXResourcesBuildPhase[resourcesPhaseUuid].files.push({
+                value: buildFileUuid,
+                comment: `${fileName} in Resources`
+            });
+        }
+
+        // 4.8 Update target to use our build phases
+        const targetObj = projObjects.PBXNativeTarget[target.uuid];
+        targetObj.buildPhases = [
+            { value: sourcesPhaseUuid, comment: 'Sources' },
+            { value: frameworksPhaseUuid, comment: 'Frameworks' },
+            { value: resourcesPhaseUuid, comment: 'Resources' },
+        ];
+
         /* ------------------------------------------------------------------ */
         /* 5. Build-settings configuration                                    */
         /* ------------------------------------------------------------------ */
diff --git a/node_modules/expo-share-intent/plugin/build/ios/writeIosShareExtensionFiles.js b/node_modules/expo-share-intent/plugin/build/ios/writeIosShareExtensionFiles.js
index c40060d..20b3bd2 100644
--- a/node_modules/expo-share-intent/plugin/build/ios/writeIosShareExtensionFiles.js
+++ b/node_modules/expo-share-intent/plugin/build/ios/writeIosShareExtensionFiles.js
@@ -41,7 +41,7 @@ async function writeShareExtensionFiles(platformProjectRoot, scheme, appIdentifi
     await node_fs_1.default.promises.writeFile(storyboardFilePath, storyboardContent);
     // ShareViewController.swift
     const viewControllerFilePath = getShareExtensionViewControllerPath(platformProjectRoot, parameters);
-    const viewControllerContent = getShareExtensionViewControllerContent(scheme, (0, constants_1.getAppGroup)(appIdentifier, parameters));
+    const viewControllerContent = getShareExtensionViewControllerContent(scheme, (0, constants_1.getAppGroup)(appIdentifier, parameters), parameters);
     await node_fs_1.default.promises.writeFile(viewControllerFilePath, viewControllerContent);
     // ShareExtensionPreprocessor.js
     const preprocessorFilePath = getPreprocessorFilePath(platformProjectRoot, parameters);
@@ -176,12 +176,14 @@ function getPreprocessorContent(parameters) {
 var ExtensionPreprocessingJS = new ShareExtensionPreprocessor();
 `;
 }
-function getShareExtensionViewControllerContent(scheme, groupIdentifier) {
+function getShareExtensionViewControllerContent(scheme, groupIdentifier, parameters) {
     let updatedScheme = scheme;
     if (Array.isArray(scheme)) {
         console.warn(`[expo-share-intent] multiple scheme detected (${scheme.join(",")}), using:${updatedScheme}`);
         updatedScheme = scheme[0];
     }
+    const keySuffix = parameters?.iosShareExtensionSharedKeySuffix || "";
+    const shareTarget = parameters?.iosShareExtensionTarget || "";
     console.warn(`[expo-share-intent] add ios share extension (scheme:${updatedScheme} groupIdentifier:${groupIdentifier})`);
     if (!updatedScheme) {
         throw new Error("[expo-share-intent] missing custom URL scheme 'expo.scheme' in app.json ! (see https://docs.expo.dev/guides/linking/#linking-to-your-app)");
@@ -189,5 +191,7 @@ function getShareExtensionViewControllerContent(scheme, groupIdentifier) {
     const content = node_fs_1.default.readFileSync(node_path_1.default.resolve(__dirname, "./ShareExtensionViewController.swift"), "utf8");
     return content
         .replaceAll("<SCHEME>", updatedScheme)
-        .replaceAll("<GROUPIDENTIFIER>", groupIdentifier);
+        .replaceAll("<GROUPIDENTIFIER>", groupIdentifier)
+        .replaceAll("<SHARE_KEY_SUFFIX>", keySuffix)
+        .replaceAll("<SHARE_TARGET>", shareTarget);
 }
